% Question 1.(a) -- Formulation, properties, hourly decomposition and its meaning
%
% SCAFFOLD ONLY: structure and section headers, no content, no equations. Fill in every
% \todo{...} with your own formulation/derivation -- nothing below is a hint at the answer,
% only at what the answer needs to contain.
%
% Source of the exact task text: course_materials/Assignment_1_Instructions.html
% ("Problem statement" section, part 1, sub-question (a)).
% Grading (Table 2, row 1.(a)): 3 pts mathematical models, 1 pt writing -- no code/numerical
% points here, so this content (once merged) is the actual deliverable for 1.(a).
%
% THIS IS A FRAGMENT, not a standalone document: no \documentclass, \usepackage or
% \begin{document} here -- your main.tex already provides all of that, plus \todo and the
% notation macros used below (\T, \qimp, \qexp, \pimp, \pexp, \cpv, \Emin, \lref, \uL, \load,
% \pvprod). Make sure main.tex's preamble also has these (add them next to your other
% \newcommand definitions, and update \pvprod if you still have the old p^{PV} version):
%
%   \newcommand{\Lmin}{L^{\min}}
%   \newcommand{\Lmax}{L^{\max}}
%   \newcommand{\PVmax}[1][t]{PV_{#1}^{\max}}
%   \newcommand{\tauexp}{\tau^{\mathrm{exp}}}
%   \newcommand{\tauimp}{\tau^{\mathrm{imp}}}
%   \newcommand{\pvprod}[1][t]{q^{\mathrm{PV}}_{#1}}   % renamed from p^{PV}_t to q^{PV}_t
%
% Indexing: t = 0, ..., 23, matching src/data_loader.py (data.hours = np.arange(24)) -- keep
% this consistent with the code you write in (e).
%
% Copy this file into your Overleaf project's `sources/` folder (e.g. as
% sources/1a_formulation.tex) and call it from main.tex, inside \section{Question 1}:
%
%   \section{Question 1}
%   \input{sources/1a_formulation}
%   \input{sources/1b_duality}
%   ...

\subsection{(a) Formulation, properties and hourly decomposition}

\subsubsection{Input data}
\begin{itemize}
    \item $\T = \{0, \dots, 23\}$: the set of hours of the day.
    \item $\Lmin$, $\Lmax$: minimum and maximum hourly load consumption (kWh/h), with
    $0 \leq \Lmin \leq \Lmax$.
    \item $\PVmax$, for every $t \in \T$: maximum available PV production in hour $t$
    (kWh/h).
    \item $p_t$: hourly electricity price in hour $t$ (DKK/kWh).
    \item $\tauexp$: export grid tariff (DKK/kWh), with the assumption
    \begin{equation}
        \tauexp < p_t, \qquad \forall t \in \T,
        \label{eq:tariff_assumption}
    \end{equation}
    i.e.\ the export tariff is always smaller than the hourly electricity price.
\end{itemize}
\begin{itemize}
    \item $\uL$: marginal consumption utility (DKK/kWh) -- a constant, the same in every
    hour.
    \item $\cpv$: PV marginal production cost (DKK/kWh) -- a constant, the same in every
    hour.
    \item $\tauimp$: import grid tariff (DKK/kWh).
\end{itemize}
The effective import and export prices used in the objective
\eqref{eq:daily_net_utility} follow directly from the market price and the tariffs: the
import tariff is added to the market price, and the export tariff is subtracted from it.
\begin{align}
    \pimp &= p_t + \tauimp, & \forall t \in \T, \label{eq:effective_import_price} \\
    \pexp &= p_t - \tauexp, & \forall t \in \T. \label{eq:effective_export_price}
\end{align}

\subsubsection{Decision variables}
\begin{itemize}
    \item $\qimp \geq 0$: electricity imported from the grid in hour $t$ (kWh/h).
    \item $\qexp \geq 0$: electricity exported to the grid in hour $t$ (kWh/h).
    \item $\load$: load consumed in hour $t$ (kWh/h); its bounds are given by constraint
    \eqref{eq:c_load_bounds} below (non-negative since $\Lmin \geq 0$: a load can only
    consume, not generate, electricity).
    \item $\pvprod$: PV production used in hour $t$ (kWh/h).
\end{itemize}

\subsubsection{Objective function}
%\todo{Write the daily net utility (surplus) to maximize, as a sum over the 24 hours ($t \in
%\T$), in terms of the variables and data defined above.}
\begin{equation}
    \max_{\{\load, \qimp, \qexp, \pvprod\}_{t \in \T}} \quad
    U = \sum_{t \in \T} \Big( \uL \, \load - \pimp \, \qimp + \pexp \, \qexp - \cpv \, \pvprod \Big)
    \label{eq:daily_net_utility}
\end{equation}

\subsubsection{Constraints}
\begin{align}
    \load &= \pvprod + \qimp - \qexp, & \forall t \in \T, \label{eq:c_balance} \\
    \Lmin \leq \load &\leq \Lmax, & \forall t \in \T, \label{eq:c_load_bounds} \\
    \qimp &\geq 0, & \forall t \in \T, \label{eq:c_import_nonneg} \\
    \qexp &\geq 0, & \forall t \in \T, \label{eq:c_export_nonneg} \\
    0 \leq \pvprod &\leq \PVmax, & \forall t \in \T. \label{eq:c_pv_limit}
\end{align}

\subsubsection{Properties of the problem}

\paragraph{Problem class.}
\todo{Is it linear, quadratic, a MILP, ...? Justify directly from the objective and
constraints written above -- don't just assert it.}

\paragraph{Convexity.}
\todo{State and justify the convexity of the problem (feasible set and objective
function), from its problem class.}

\paragraph{Feasibility.}
\todo{Give the condition(s) on the input data under which the problem is feasible (i.e.
under which a point satisfying every constraint exists).}

\paragraph{Boundedness.}
\todo{Give the condition(s) on the input data under which the objective is bounded (think
about what happens if a costless arbitrage between buying and selling were possible).}

\paragraph{Size.}
\todo{Number of variables and number of constraints, as a function of the 24-hour
horizon.}

\subsubsection{Hourly decomposition}

\paragraph{Decomposition.}
\todo{Show that the daily problem decomposes into 24 independent hourly problems: identify
every term of the objective and every constraint, and check whether any of them links two
different hours together.}

\paragraph{The hourly problem.}
\todo{Write out the single-hour problem explicitly (its own objective and constraints, for
one generic hour $t$).}

\paragraph{Computational meaning.}
\todo{What does this decomposition change about how the problem can be solved (size of the
problem actually being solved, parallelisability, ...)?}

\paragraph{Economic meaning.}
\todo{What does this decomposition say about the structure of the consumer's
decision-making across the day?}
